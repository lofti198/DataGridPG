<Window x:Class="DataGridPG.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataGridPG"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Button -->
        <Button Grid.Row="0" Name="FillTableButton" Content="Fill table" Margin="10" Click="FillTableButton_Click"/>

        <!-- Button -->
        <Button Grid.Row="1" Name="MessageBoxButton" Content="MessageBox" Margin="10" Click="MessageBoxButton_Click"/>
        <!-- DataGrid -->
        <DataGrid Grid.Row="2" Margin="10" x:Name="dataGrid"/>
    </Grid>
</Window>
